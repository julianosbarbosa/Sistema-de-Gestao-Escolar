@model IEnumerable<SistemaControleAlunos.Models.Grupos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Professor
        </th>
        <th>
            Disciplina
        </th>
        <th>
            Estudantes
        </th>
        <th>
            
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Professor.NomeCompleto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GroupoDetalhes.Count)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GrupoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GrupoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GrupoId })
        </td>
    </tr>
}

</table>
